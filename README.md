# The NOTAMator

A Rails app that parses input data and displays it on a results page.

## How to run it

 `cd NOTAM_exercise`

  `bundle install`
  
  `rails server`
  
 `open http://localhost:3000`
